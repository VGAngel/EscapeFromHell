# Everything in this folder is secrets — keystores, passwords, env
# files. The folder itself is checked in (so the layout is documented),
# but its contents never are. The single exception is the .example
# template that shows the expected variables without any real values.
*
!.gitignore
!keystore.env.example
!README.md
